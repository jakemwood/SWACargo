class Airport:
	def __init__(self, code, zone):
		self.code = code
		self.zone = zone

class Service:
	def cost(waybill):
		return 0

class Waybill:
	def __init__(self, packages, origin, destination):
		self.packages = packages
		self.origin = origin
		self.destination = destination